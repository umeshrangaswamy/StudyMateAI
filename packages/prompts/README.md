# StudyMateAI Prompts Package

This is a Python library containing system prompt configurations, instructions, and prompt templates for StudyMateAI agents (Orchestrator, Physics SME, Chemistry SME, Quiz Generator, Evaluator).

## 🚀 Usage

Install in editable mode for backend use:
```bash
pip install -e packages/prompts
```
