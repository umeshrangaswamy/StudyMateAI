from setuptools import setup, find_packages

setup(
    name="studymateai-prompts",
    version="1.0.0",
    packages=find_packages(),
    description="System prompt instructions and template packages for StudyMateAI.",
    install_requires=[],
)
